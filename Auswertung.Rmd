---
title: "Influence of personal and social benefits on the intention to donate blood in the context of age and the constructs of the Self-Determination Theory - Master Thesis"
author: "Tim Riester"
output: html_document
date: "2024-03-21"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.


The following analyses are reported in our method study and sorted according to the order in which they appear in the corresponding report.

```{r include=FALSE, warning = FALSE, message = FALSE}
#load packages
library(psych)
library(expss)
library(plyr)
library(dplyr)
library(ggplot2)
library(ggpubr)
library(reshape2)
library(tidyr)
library(Hmisc)
library(sjPlot)
library(table1)
library(DescTools)
library(moments)
library(corrplot)
library(QuantPsyc)
library(coin)
library(corrr)
library(knitr)
library(kableExtra)
library(tidyverse)
library(apaTables)
library(sandwich)
library(carData)
library(car)
library(Rmisc)

```
#load Data
```{r}
data  <- read.csv("data_project_1013280_2024_03_14.csv", sep = ";")
```

#Data preperation

##Age Groups
```{r}
data$AGEGROUP <- NA
data$AGEGROUP <- factor(
  data$AGEGROUP,
  levels = c(1,2),
  labels = c("18-29", "30-75")
)

data$AGEGROUP[which(data$ALTER >= 18 & data$ALTER <=29 )] <- "18-29"
data$AGEGROUP[which(data$ALTER >= 30 & data$ALTER <= 75 )] <- "30-75"

summary(data$AGEGROUP)

agegroupwave <- c(
  "1" = "18-21",
  "2" = "22-25"
)

data$GENDER <- factor(
data$GENDER,
levels = c(1,2,3,4),
labels = c("Weiblich", "Männlich","Divers","Keine Angabe")
)

data$EDUCATION <- factor(
data$EDUCATION,
levels = c(1,2,3,4,5,6,7,8),
labels = c("Hauptschulabschluss","Mittlere Reife","Bachelor","Fachhochschulreife","	Allgemeine Hochschulreife","Bachelor","Master","Andere")
)

data$Condition <- factor(data$c_0001, levels = c(1,2,3)
labels = c("Individual Benefit", "Social Benefit", "Control Group")
```




